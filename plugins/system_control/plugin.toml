id = "system_control"
name = "System Control"
description = "Lock, suspend, hibernate, reboot, or power off the computer"
enabled = true
entrypoint = "system_control_plugin.py"
lifecycle_mode = "on_call"

provides_general_suggestions = false
prefix = "@system"
require_prefix = true
build_dependencies = ["python3", "systemctl", "loginctl"]
capabilities = ["action"]
icon = "system-shutdown"
