name = "file_finder"
description = "File search across user directories with SQLite index"
enabled = true
entrypoint = "file_finder"
lifecycle_mode = "on_demand_persistent"

provides_general_suggestions = false
prefix = "@f"
require_prefix = false
build_dependencies = ["go"]
capabilities = ["suggest"]
