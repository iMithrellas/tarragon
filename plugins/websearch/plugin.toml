name = "websearch"
description = "Quick web search via prefix shortcuts"
enabled = true
entrypoint = "websearch_plugin.py"
lifecycle_mode = "on_demand_persistent"

provides_general_suggestions = true
prefix = ""
require_prefix = false
build_dependencies = ["python3"]
capabilities = ["suggest"]
